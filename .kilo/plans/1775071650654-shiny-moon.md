$START_DEV_PLAN

**PURPOSE:** Аудит соответствия текущего набора инструментов (Tools) и загруженных промптов Kilo Code спецификациям фреймворка разработки.

---

### 1. Draft Code Graph (Черновой Граф Кода)

```xml
<DraftCodeGraph>
  <Audit_System_Info FILE=".kilo/plans/audit.md" TYPE="AUDIT_PLAN">
    <annotation>План аудита системных инструментов и промптов.</annotation>
  </Audit_System_Info>
</DraftCodeGraph>
```

---

### 2. Step-by-step Data Flow (Пошаговый Поток Данных)

1.  **Инвентаризация Tools:** Сбор списка всех доступных системных инструментов (API).
2.  **Анализ Формата Правок:** Изучение спецификации инструмента `edit` и его отличий от `apply_diff`.
3.  **Микрозадача (Тестовый проход):** Подготовка точечной правки для `.kilocode/rules/rules.md`. (Выполнено)
4.  **Делегирование (Swarm Execution):** Запуск субагента `general` для применения этой микрозадачи. (Выполнено основным агентом)
5.  **Верификация:** Проверка корректности замены и того, что субагент успешно справился с инструментом `edit`.

---

### 3. Acceptance Criteria (Критерии Приемки)

- [ ] Устаревшие инструкции по `apply_diff` в `.kilocode/rules/rules.md` заменены на актуальные рекомендации по `edit`.
- [ ] Субагент успешно выполнил правку, не затронув остальную структуру файла.
- [ ] Основной агент подтвердил результат.

---

### 4. Swarm Memory & Context Preservation (Сохранение контекста)

**PURPOSE:** Фиксация критического состояния системы перед сжатием контекста (Compaction).

**KEY_FACTS:**
- **Система:** Kilo Code 7.1.17 (Open Code).
- **Tool Mapping:** 
  - `read_file` -> `read`, `execute_command` -> `bash`, `update_todo_list` -> `todowrite`.
  - `apply_diff` -> `edit` (точная замена строк).
  - `switch_mode` -> `task` (делегирование).
- **Infrastructure:** Файл `kilo.json` в корне проекта активирует разрешения для субагентов.
- **Swarm Roles:** 
  - **Code Agent**: Пишет код + `tests/test_guide.md`.
  - **QA Agent**: Выполняет диагностику по правилам `.kilocode/rules-qa/rules.md` и выдает отчет Архитектору.
  - **Architect**: Оркестрирует итерации через `task`.

**CRITICAL_FILES:**
- `.kilocode/rules/rules.md` (Обновлен: `edit` instructions)
- `.kilocode/rules-code/rules.md` (Обновлен: `todowrite`, `bash`, `test_guide` instructions)
- `.kilocode/rules-architect/rules.md` (Обновлен: Swarm/Delegation logic)
- `.kilocode/rules-qa/rules.md` (Создан: Diagnostic Trio protocol)
- `kilo.json` (Создан: Permissions)

$END_DEV_PLAN
