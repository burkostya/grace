$START_DEV_PLAN

**PURPOSE:** Обновление глобальных инструкций (провптов) для перевода семантической разметки и логов на английский язык с целью оптимизации токенов и устранения проблем с кодировкой в консоли Windows.

---

### 1. Draft Code Graph (Черновой Граф Кода)

```xml
<KnowledgeGraph>
  <PromptOptimization_py FILE=".kilocode/rules/rules.md" TYPE="CORE_RULES_MODULE">
    <annotation>Обновление базовых правил разметки и шаблонов на английский язык.</annotation>
  </PromptOptimization_py>
  
  <CodeProcessOptimization_py FILE=".kilocode/rules-code/rules.md" TYPE="CODE_PROCESS_MODULE">
    <annotation>Обновление процесса генерации кода и тестов для вывода логов на английском.</annotation>
  </CodeProcessOptimization_py>
</KnowledgeGraph>
```

---

### 2. Step-by-step Data Flow (Пошаговый Поток Данных)

1.  **Refactor rules.md (Global):**
    *   Обновление `=== АБСТРАКТНЫЙ СЕМАНТИЧЕСКИЙ ШАБЛОН ===`. Перевод полей `PURPOSE`, `SCOPE`, `INPUT`, `OUTPUT` и т.д. на английский.
    *   Обновление `=== ИНСТРУКЦИИ LOG DRIVEN DEVELOPMENT 2.0 (LDD) ===`. Перевод описаний уровней `IMP` на английский.
    *   Обновление `=== ONE-SHOT ПРИМЕР ===`. Полный перевод примера `check_ai_libs.py` (docstrings, логи, контракты) на английский.
2.  **Refactor rules-code/rules.md:**
    *   Обновление первичной классификации задачи (`ОПРЕДЕЛЕН ТИП ПРОЕКТА` -> `PROJECT_TYPE_DEFINED`).
    *   Перевод названий шагов (`STUDY_THE_PLAN`, `IMPLEMENT_CODE`) в логах `[ROUTING]` на английский.
    *   Обновление примера теста (`One-Shot Пример правильного теста`) с переводом всех строк логов и `print` на английский.
3.  **Refactor graph_generation_prompt.md:**
    *   Обновление инструкций по генерации XML-графа. Перевод `keywords`, `terms`, `annotation` и названий типов связей в примерах на английский.
4.  **Verification:** Проверка консистентности инструкций во всех измененных файлах.

---

### 3. Acceptance Criteria (Критерии Приемки)

- [ ] Все семантические теги и поля контрактов в `rules.md` используют английские ключи (например, `PURPOSE` -> `PURPOSE`, описание -> на английском).
- [ ] Логи в примерах кода используют только ASCII символы (английский язык).
- [ ] Инструкции по выводу в консоль (`[ROUTING]`, `[IMP:...]`) во всех промптах переведены на английский.
- [ ] XML-граф в инструкциях использует английские типы и аннотации в примерах.
- [ ] Сохранена структура разметки `# START_BLOCK` / `# END_BLOCK`.
- [ ] Устранены все упоминания кириллицы в шаблонах генерации кода.

$END_DEV_PLAN

