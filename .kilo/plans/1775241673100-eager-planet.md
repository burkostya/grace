# План аудита и исправления `.kilocode\skill\mode-architect\SKILL.md`

## Проблема
Файл содержит ссылки на несуществующие ресурсы (директории `.kilocode/rules-architect/`, файл `rules-code.md`) и недостаточно четко указывает на необходимость использования `skill()` для получения протоколов и шаблонов.

## Предлагаемые изменения

### 1. Обновление Шага 1 (THINK_AND_CLARIFY)
- **Локация:** Строка 23.
- **Изменение:** Заменить рекомендацию изучать правила в папке `.kilocode/` на вызов соответствующих скиллов.
- **Новый текст:** `0. **Изучение возможностей исполнителей:** Перед началом проектирования ознакомьтесь с правилами режимов через `skill(name="mode-code")`, `skill(name="mode-debug")` и Orchestrator. Это необходимо для понимания ограничений и возможностей субагентов.`

### 2. Обновление Шага 5 (DELEGATE_IMPLEMENTATION)
- **Локация:** Строка 60.
- **Изменение:** Удалить ссылку на несуществующий `rules-code.md`.
- **Новый текст:** `Используйте инструмент `task` для делегирования реализации субагенту `general`. Промпт должен соответствовать протоколу из `skill(name="mode-code")`.`

### 3. Обновление Раздела 4 (Когнитивный Прайминг)
- **Локация:** Строки 93 и 96.
- **Изменение:** Заменить пути к файлам шаблонов на упоминание скиллов.
- **Новый текст для строки 93:** `*   **О шаблоне Development Plan (`skill(name="devplan-protocol")`):**`
- **Новый текст для строки 96:** `*   **О шаблоне общих документов (`skill(name="document-protocol")`):**`

### 4. Общие улучшения
- Проверить, чтобы все упоминания `DevelopmentPlan.md` и `business_requirements.md` соответствовали текущей структуре (обычно создаются в `.kilo/plans/`).
- Убедиться, что инструкции по вызову `skill(name="devplan-protocol")` и `skill(name="document-protocol")` на Шаге 4 (строка 51) остаются акцентированными.

## Ожидаемый результат
Файл `mode-architect/SKILL.md` станет актуальным, не будет содержать битых ссылок и будет четко направлять агента на использование системы скиллов для получения контекста и шаблонов.
